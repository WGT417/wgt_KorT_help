# 이 저장소에서 일하는 방법

## 용어 카드 검수·보강은 사람이 읽듯이 한다

**`scripts/write_term_notes.py`를 실행하지 않는다.** 사용자가 그 스크립트 이름을 대며
직접 시키기 전까지는 돌리지 않는다. 돈이 나가고(전체 실행 약 $4.7), 품질도 본문을
직접 읽고 쓰는 쪽이 낫다는 것이 2026-09-23 라운드에서 확인되었다. 카드를 검사하고
채우는 일은 **네가 개론서 본문을 읽고 판정해서** 한다.

### 무엇으로 쓰는가

- **새 내용은 `concepts/*.json`의 개념 항목으로 쓴다.** 풍부한 서술 한 편이 별칭을 통해
  카드 여러 장을 한 번에 살린다(‘이다’의 문법 범주 한 항목이 빈 카드 13장을 덮었다).
- **한두 문장 보완이면** `data/term-notes.json`의 풀이를 고쳐도 된다.
- 형식은 `scripts/check_concepts.py`가 검사한다. 필수 키 `id, label, parent, aliases,
  summary, sections, sources, review`, sections 3개 이상, 본문 공백 제외 400자 이상,
  항목은 `{"label","text","examples","refs","sub"}`(중첩 2단계까지), `refs`는 sources 인덱스,
  sources는 `{"book","pdf_page","status","note"}` — **printed_page가 아니라 pdf_page**.

### 쓸 때 지키는 것

- **반드시 서재의 책 본문을 읽고 쓴다.** 기억으로 쓰지 않는다. 근거 쪽을 `sources`에 적고,
  본문으로 확인한 것만 `status: "verified"`로 둔다. **책이 말하지 않으면 쓰지 않는다.**
- 교사·임용 관점으로, 한 흐름으로 읽히게 쓴다(간접 인용절이면 “어떤 절인가 → 어떤 표지가
  붙는가 → 무엇이 중화되는가 → 직접 인용과 무엇이 다른가”).
- **책마다 견해가 다르면 차이를 드러내고 어느 책의 관점인지 밝힌다.** 예: ‘이다’를 학교 문법은
  서술격 조사로, 〈표준 문법〉은 형용사로, 〈한국어문법총론 1〉은 의존 형용사가 낫다면서도
  설명의 편의로 서술격 조사로 기술한다고 스스로 밝힌다.
- **깨진 글자를 추측으로 복원하지 않는다.** 고전 자료의 옛한글은 OCR 단계에서 이미 무너져
  있어 되살릴 수 없다.
- 20~40장씩 묶어 작업하고, 묶음마다 무엇을 고쳤는지 **수치로 보고**한다.

### 빈 카드를 살리는 방법 — 효과 순서대로

1. **근거 쪽 겹침(가장 정확하다, 먼저 쓸 것).** 카드가 색인된 바로 그 쪽을 어떤 개념 항목이
   이미 `sources`에 들고 있고 그 항목 본문이 그 용어를 쓰면, 그 항목이 그 카드를 설명하고
   있는 것이다. 별칭만 넣으면 살아난다.
2. **접미 형태 변이.** 카드 이름에서 절·문·법·음·성·적·론·화·어·사 같은 접미를 떼거나 붙여
   개념 이름이 되면 후보다.
3. **빈 카드를 근거 쪽으로 묶어 덩어리 찾기.** 한 챕터에 몰린 빈 카드 무리가 새 개념 항목 거리다.

### 별칭을 넣기 전에 반드시 근거 쪽 본문을 읽는다

이름만 보고 넣었으면 틀렸을 실제 사례들이다. 같은 낱말이 책마다 다른 뜻이다.

| 카드 | 이름만 보면 | 본문이 말하는 것 |
|---|---|---|
| 결속 | 담화의 결속성 | 화법 교육론 37쪽 “화법이 참여자들을 **결속시키는 힘**” |
| 전사 | 작문의 전사하기 | 한국 고전소설의 세계 229쪽 한문본을 한글본으로 옮기는 **필사** |
| 초점 | 초점화 | 우리말문법론 170쪽 격조사 생략의 **정보 초점** |
| 표제 | 사전 표제어 | 작문 교육론 320쪽 **기사문의 표제** |
| 수정·조정 | 고쳐쓰기·조정하기 | 한국어표준문법 674·650쪽 대화의 **repair**와 담화의 **mediation** |
| 회피 | 갈등 처리 전략 | 작문 교육론 177쪽 **쓰기 불안** |
| 하게 | 하게체 | 우리말문법론 326쪽 부사어를 만드는 **어미 ‘-게’** |
| 방각소 | 방각소설의 깨진 표기 | 한국 고전소설의 세계 233쪽 “서책을 방각하는 곳” — **별개 용어, 합치면 안 된다** |

- 흔한 낱말은 넣지 않는다(`concepts.TOO_COMMON` 참고). 인명은 개념 항목의 별칭으로 넣지 않는다.
- **카드 이름에 괄호나 빗금이 있으면 `key_of`가 지우지 않으므로 별칭도 그 꼴 그대로** 넣는다:
  `개념어 (concept terms)`, `퍼스나(persona)`, `도약(saccade)`, `이익/비용 쟁점`.
  띄어쓰기는 지워지므로 상관없다.
- 카드를 물리적으로 합치지 않는다. 개념 항목의 `aliases`에 두 이름을 모두 넣는 것이 이
  저장소의 방식이다. `term_index.LABEL_FIXES`는 **같은 말의 깨진 표기**를 합칠 때만 쓴다.

### 다시 하지 말 것 (측정해 보고 버린 것)

- **`noise()`를 풀이에 적용하지 말 것.** 본문용이라 풀이의 연도 숫자(“1927년 창간된”)에 걸린다.
  첫 전수조사 42건 중 26건이 그 헛짚음이었다. 깨짐 검사는 **책 정의에만** 한다.
- **“인용에 표제어가 없는 풀이”를 오류 후보로 삼지 말 것.** 338장 중 30장을 읽었는데 거의 다
  정확했다. 표기 차이일 뿐이다(연극성 → “연극의 대본”, 파생작 → OCR “파생 직”).
- **`alias_only_unused`는 오류가 아니다.** 별칭으로 붙었는데 개념 본문이 그 용어를 안 쓰는
  131건은 대부분 표기 차이다(‘내적 변화 파생’ ↔ 본문의 “내적 변화에 의한 파생”).

## 명령

파이썬은 `%USERPROFILE%\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`.
한글 출력에 필요한 `PYTHONIOENCODING=utf-8`과 `CLOUDSDK_PYTHON`은 `.claude/settings.json`에
이미 들어 있다(다른 도구로 `subprocess`를 띄울 때는 직접 넘겨야 한다).

```
python scripts/check_concepts.py                          # 개념 항목 형식 검사 (몇 초)
python scripts/audit_term_cards.py                        # 전수조사 (약 24분, data/term-card-audit.json)
python -m unittest tests.test_app tests.test_text_pipeline # 테스트 81개 (한가할 때 약 90초)
```

근거를 찾을 때: `term_index._found(key_of(용어))`로 그 용어의 쪽을, `data/library.sqlite3`의
`passages`(kind='body')에서 문단을 읽는다. 도서관 전체 검색은 `search` 표(FTS, trigram).
화면에 보이는 글은 `text_pipeline.correct_display`를 거친 것이다.

**로컬 서버(8799 등)를 켜 둔 채 다른 DB 작업을 하지 말 것.** 화면이 15초마다 `/api/status`를
불러 DB를 크게 훑는다. 확인이 끝나면 끈다.

**테스트는 기계 부하에 약하다.** 같은 코드로 92초 만에 전부 통과하다가, 감사 직후에는
`PublicModeTests`가 전부 429로 깨진다(앞 시험의 생성 스레드가 `AI_LOCK`을 쥔 채 남는다).
클래스별로 격리하면 통과한다. 코드를 의심하기 전에 기계를 쉬게 하고 재실행한다.

## 배포

배포는 **사용자에게 물어보고** 한다.

- `concepts/*.json`이나 파이썬만 고쳤으면 **Cloud Run 배포 하나면 된다**(빌드 7~11분).
  `PATH`에 gcloud bin, `CLOUDSDK_PYTHON` 설정 후 `python scripts/cloud.py deploy`.
- `data/`를 고쳤으면 **먼저** `python scripts/cloud.py push-data`(dry-run으로 비교부터).
- `dist/`(화면)를 고쳤으면 `firebase deploy --only hosting`도.
- 확인은 직접 `*.run.app`이 아니라 <https://kor-teacher-help.web.app/api/…>로 한다.
  이 노트북에서 run.app 직접 접속은 TLS가 끊긴다.
